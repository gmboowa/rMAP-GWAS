include required(classpath("application"))

system {
  max-concurrent-workflows = 1
}

backend {
  providers {
    Local {
      config {
        concurrent-job-limit = 1
      }
    }
  }
}

call-caching {
  enabled = false
}

docker {
  hash-lookup {
    enabled = false
  }
}
